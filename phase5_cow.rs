//! Phase 5 copy-on-write placeholder module.
//! This repository currently keeps CoW runtime optimizations behind a feature flag while implementation is in progress.

/// Returns whether optional Phase 5 CoW optimizations are available.
#[must_use]
pub fn is_available() -> bool {
    false
}
