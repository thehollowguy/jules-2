//! Phase 3 JIT placeholder module.
//! This repository currently keeps JIT behind a feature flag while implementation is in progress.

/// Returns whether the optional Phase 3 JIT backend is available.
#[must_use]
pub fn is_available() -> bool {
    false
}
